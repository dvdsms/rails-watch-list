<h1>Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ul>
<p><%= link_to "Add a list", new_list_path %></p>
<p><%= link_to "Add a bookmark", new_bookmark_path %></p>
<p><%= link_to "Go to bookmarks", bookmarks_path %></p>
